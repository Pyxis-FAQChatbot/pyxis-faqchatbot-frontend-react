# pyxis-faqchatbot-frontend-react

1. gitignore로 설정된 node_modules 생성필요

 - 터미널 통해 react, axios, lucide-react 설치(설치예 : npm install react)
 - 다른환경에서 확인하지 못했으나 vite 의 설치도 필요할 수 있음

2. gitignore로 설정된 '.env.developement'의 작성필요
 - src/ 경로에 파일명 '.env.developement' 생성
 - 파일내에 VITE_API_BASE_URL= (백엔드url 주소) 작성
